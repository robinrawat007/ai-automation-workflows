// Legacy server-side workflow scanner (used by old API routes).
// The site is now fully static and uses build-time generated JSON under `public/`.
// This module intentionally exports nothing so it won't be bundled.
export {}
